export const GameFinishedView = () => {
  {
    /* {room?.details.gameStatus === GameStatus.Finished && (
        <div className="row w-100">
          <div className="col my-3 d-flex flex-column align-items-center justifiy-content-center p-2">
            <p className="text-white">
              Status{" "}
              <span className="text-warning">
                {room.winner?.status}
              </span>
              : {JSON.stringify(room.winner?.winners)}
            </p>
            <button
              className="btn btn-primary"
              onClick={handleClaimProcess}
            >
              Claim
            </button>
          </div>
        </div>
      )} */
  }

  return <div>GameFinishedView</div>;
};
